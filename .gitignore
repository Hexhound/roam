# rust
/target/
**/*.rs.bk
Cargo.lock

# wasm build output (generated by crates/roam-wasm/tests/js/run.sh). The
# fixtures under tests/fixtures/ ARE committed — they are the interop contract.
/crates/roam-wasm/pkg/
# Same, for the headless-browser harness (crates/roam-wasm/tests/browser/run.sh).
/crates/roam-wasm/tests/browser/pkg/

# nix / devenv / direnv
.direnv/
.devenv/
.devenv.flake.nix
devenv.local.nix
.env

# editor / os
*~
.DS_Store
